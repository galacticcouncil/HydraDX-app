import { ActionTypes as walletTypes } from '@/store/modules/wallet/action-types';
import { ActionTypes as rootATypes } from '@/store/modules/root/action-types';

export const AllActionTypes = {
  wallet: walletTypes,
  root: rootATypes,
};
